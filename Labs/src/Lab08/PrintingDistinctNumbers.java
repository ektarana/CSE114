package Lab08;

/*(Printing distinct numbers) Write a program that reads in ten numbers 
 * and displays distinct numbers (i.e., if a number appears multiple times, it is displayed only once). 
 * Hint: Read a number and store it to an array if it is new. 
 * If the number is already in the array, discard it. 
 * After the input, the array contains the distinct numbers.
 */
import java.util.Scanner;

public class PrintingDistinctNumbers {
	public static void main(String[] args) {
		Scanner input = new Scanner(System.in);
		int[] distinctResult = new int[10]; 
		int num = input.nextInt(); 
		
		
		for(int i=0; i<distinctResult.length; i++){
			if(distinctResult[i] != num){
				
			}
			
			
		}
		
		
		
	}
}